# Hub global skills

Place vault-aware skills that operators need in **personal** Cursor skills (`~/.cursor/skills/`), not only in the hub checkout.

```bash
bash scripts/install-global-skills.sh
bash scripts/install-global-skills.sh --also-claude   # optional Claude Code dir
```

See silas-agent `global-skills/` for a filled example (esignatures + secrets).
